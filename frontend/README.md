```markdown
# Notes App — Frontend (Next.js + TypeScript + Tailwind)

Requirements:
- Node.js 18+
- npm

Install & run:
1. cd frontend
2. npm install
3. npm run dev
   - App available at http://localhost:3000

Notes:
- API base URL: http://localhost:8080/api/notes
- Uses App Router (app/), client components for interactivity, TailwindCSS for styling.
```